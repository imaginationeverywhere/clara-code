# Prompt — Desktop IDE (Tauri) + `.dmg` + R2

**TARGET REPO:** `imaginationeverywhere/clara-code`

## Goal

- `desktop/`: Tauri app with editor + Clara conversation panel; same voice loop as CLI via `@imaginationeverywhere/clara-voice-client`.
- Build **macOS `.dmg`**; upload artifacts to **Cloudflare R2** (or equivalent) linked from `claracode.ai` download.
- **Refs:** `desktop/README.md`, directive deliverable 3; wire when `POST /voice/converse` + API key land.
