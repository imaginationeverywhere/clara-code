/**
 * Placeholder for future per-agent skill attachments (DB-backed).
 * Call sites keep a stable import path; implementation can store rows later.
 */
export async function attachSkills(_agentId: string, _skills: string[]): Promise<void> {
	return;
}
