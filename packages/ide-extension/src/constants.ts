/** Default Hermes gateway; not exposed via workspace settings. */
export const DEFAULT_GATEWAY_URL = "https://info-24346--hermes-gateway.modal.run";

/** SecretStorage key for optional dev override of the gateway URL. */
export const GATEWAY_SECRET_KEY = "clara.gatewayUrl";
