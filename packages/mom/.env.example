# Slack (required for mom bot)
# MOM_SLACK_APP_TOKEN=
# MOM_SLACK_BOT_TOKEN=

# Optional: Clara Gateway (Hermes) — mom logs reachability on startup; agent run loop still uses Anthropic until fully wired
# HERMES_GATEWAY_URL=https://info-24346--hermes-gateway.modal.run
