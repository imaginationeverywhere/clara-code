# Changelog

## [Unreleased]

### Added

- Initial unscoped `clara` metapackage: `bin` delegates to `@clara/cli` (same as `npx` / `npm i -g @clara/cli` for users who need the public name `clara` on PATH).
