# Changelog

## [Unreleased] - 2026-04-25

### Added

- **Shim and tests** — `bin/clara.mjs` forwards to the workspace CLI; `test/shim.test.mjs` (node:test) and `"test": "node --test test/*.test.mjs"` in `package.json`.
