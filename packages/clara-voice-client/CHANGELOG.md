# Changelog

## [Unreleased]

### Added

- Initial release: `postVoiceConverse` and `resolveConverseUrl` for `POST {base}/voice/converse` with offline-safe error handling; Node `readGreetingFromCache` / `writeGreetingToCache` with XDG or `~/.cache/clara-code` layout.
