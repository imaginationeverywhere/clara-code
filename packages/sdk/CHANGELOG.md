# Changelog

## [Unreleased]

### Added

- Initial `@claracode/sdk` release: `createClient`, Bearer-auth requests to `hermesUrl`, `ask`, SSE `stream`, `startVoice` / `VoiceSession`, `createAgent` with agent-scoped `ask`/`stream`; dual ESM (`.mjs`) + CJS (`.js`), declarations; `scripts/hermes-stub.mjs` and `test/ask.test.ts` for local verification.
