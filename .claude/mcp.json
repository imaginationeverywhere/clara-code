{
  "mcpServers": {
    "knowledge-graph": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-knowledge-graph",
        "--memory-path",
        "${PROJECT_ROOT}/.aim/"
      ]
    }
  }
}
