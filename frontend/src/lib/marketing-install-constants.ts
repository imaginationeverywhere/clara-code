/**
 * Public marketing copy for install surfaces. CLI is not published to npm yet.
 * Do not use `npx github:…` against the monorepo root (no bin; breaks users).
 */
export const MARKETING_GITHUB_REPO = "https://github.com/imaginationeverywhere/clara-code";

export const CLI_INSTALL_PLACEHOLDER = "npm install -g @clara/cli";
