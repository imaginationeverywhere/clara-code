# Code Review — Prompts 11 + 12 + 13
**Date:** 2026-04-14
**Branch:** develop (PRs #21, #22, #23 — merged in order #22 → #23 → #21)
**Reviewer:** Carruthers (automated via /review-code)
**Scope:** Developer Portal Frontend + First-Party Talent Subgraphs + Coverage Fixes (corrective from prior B+ review)

---

## ⚠️ COVERAGE REQUIREMENT CHECK

**Status:** ✅ PASS — all changed source files above 80%, global thresholds met for lines/statements

| Metric | Value | Status |
|--------|-------|--------|
| Tests Run | 166 (backend) + 6 (subgraphs) | — |
| Tests Passed | 172 | ✅ |
| Tests Failed | 0 | ✅ |
| Overall Line Coverage | 81.3% | ✅ |
| Overall Statement Coverage | 80.5% | ✅ |
| Overall Branch Coverage | 63.1% | ⚠️ pre-existing |

### Per-File Coverage — Changed Files (PR #21)

| File | Lines | Stmts | Branch | Status |
|------|-------|-------|--------|--------|
| `src/config/models.ts` | 96.2% | 96.2% | 87.5% | ✅ |
| `src/routes/user-usage.ts` | 100.0% | 100.0% | 85.7% | ✅ |
| `src/utils/request-tier.ts` | 100.0% | 100.0% | 100.0% | ✅ |

**Note on branch threshold:** The global branch coverage (63.1%) remains below the Jest threshold of 65%. This is **pre-existing and unrelated to these PRs** — PR #21 improved branch coverage from 59.8% → 63.1%. The culprits are earlier talent registry routes (`talent-registry.routes.ts` at 28.1%, `developer-program.routes.ts` at 22.2%, `clara-core/resolvers.ts` at 13.0%) added in prompts 06-09. These require separate corrective prompts.

---

## Executive Summary

- **Files Reviewed:** 21 changed source files across 3 PRs
- **Tests:** ✅ 172/172 passing (166 backend + 3 gateway-connect + 3 clerk-auth)
- **Overall Coverage:** 81.3% lines / 80.5% statements ✅
- **Issues Found:** 3 (0 critical, 0 high, 1 medium, 2 low)
- **Lines Changed:** +20,033 / -15,871 (bulk of deletions are moved prompt files in PR #23)
- **Overall Grade:** A-
- **Review Status:** ✅ APPROVED — all three PRs merged to develop

---

## ✅ Corrective Issues Resolved (from prior B+ review)

### H1 — `request-tier.ts` real-function tests: ✅ FIXED
`backend/src/__tests__/utils/request-tier.test.ts` now exercises the real `resolveRequestTier()`:
- `sk-clara-` found → returns tier ✅
- `sk-clara-` not found → "free" ✅
- `cc_live_` hash validates → returns tier ✅
- `cc_live_` hash fails → "free" ✅
- `cc_live_` no candidates → "free" ✅
- Clerk session with subscription → returns tier ✅
- Clerk session, no subscription → "free" ✅
- No Authorization header → "free" ✅
- `auth()` throws → "free" (graceful fallback) ✅

Coverage: **100% lines / 100% branches** (was 20%).

### H2 — `user-usage.ts` missing branches: ✅ FIXED
Both missing paths now tested:
- `!userId` → 401 `{ error: "Unauthorized" }` ✅
- `voiceUsageService.getUsage` throws → 500 `{ error: "Failed to load usage" }` ✅

Coverage: **100% lines / 85.7% branches** (was 78.9%).

### M1 — Hardcoded Modal URL removed: ✅ FIXED
`backend/src/config/models.ts:15`:
```typescript
// Before (violation):
const VOICE_FALLBACK = process.env.CLARA_VOICE_URL || "https://quik-nation--...modal.run";

// After (correct):
const VOICE_FALLBACK = process.env.CLARA_VOICE_URL ?? "";
```
Empty-backend guard also added in `resolveModel()`:
```typescript
if (!model.inferenceBackend) {
    throw new Error(`Clara voice service is not configured (${modelName})`);
}
```
`jest.setup.js` sets `CLARA_VOICE_URL` to a test URL — models.test.ts continues to work. ✅

---

## PR #22 — Developer Portal Frontend (`packages/developer-portal/`)

### Architecture
- Next.js 15 + Tailwind, deployed to `developers.claracode.ai` via Cloudflare Pages
- API key auth via `localStorage("clara_api_key")` — no Clerk dependency in portal ✅
- `useAuth.ts` redirects to `claracode.ai/sign-in?redirect=...` when no key — user lands back after auth ✅

### Security
- Subgraph URL displayed as "never shown publicly" in the UI ✅ — aligns with talent isolation model
- `lib/api.ts` sends `Authorization: Bearer <apiKey>` on every request — consistent with backend middleware ✅
- Name sanitization: `.toLowerCase().replace(/[^a-z0-9-]/g, "")` enforces valid slug format ✅

### Data
- Price calculation: `Math.round(parseFloat(form.priceMonthly) * 100)` → cents before API call ✅
- `maxLength={160}` on description textarea ✅

### No backend test changes — frontend-only package. Coverage requirement applies to backend only.

---

## PR #23 — First-Party Talent Subgraphs

### Security — Token Verification
`verifyClaraServiceToken()` in `@claracode/marketplace-sdk`:
- Uses `timingSafeEqual` (node:crypto) for constant-time comparison ✅
- Throws if `CLARA_SERVICE_TOKEN` is unset — fail-fast, no silent bypass ✅
- Applied on **every request** in Apollo Server context function ✅
- All requests rejected if token missing or invalid (returns `UNAUTHENTICATED`) ✅

### Talent Isolation
`parseClaraContext()` passes to resolvers:
- `sessionToken`, `voiceCommand`, `requestedAt` — context Clara chooses to share ✅
- No `userId`, `email`, `subscription` data — subscriber identity never reaches talent code ✅

### Rover Compose Workaround
`apollo-router/gateway-connect.graphql` and `apollo-router/clerk-auth.graphql` committed as static schema files. `supergraph.yaml` uses `schema.file:` instead of introspection. This correctly works around the rover limitation (can't introspect token-gated subgraphs). ✅

### Test Coverage (Subgraph Packages)
Both packages use Jest + supertest with real Apollo Server:

**gateway-connect** (3/3 passing):
- Valid service token → `gatewayHealth.status === "ok"` ✅
- Invalid token → Unauthorized error ✅
- Session context headers propagated correctly ✅

**clerk-auth** (3/3 passing):
- Valid token → `authStatus` resolver works ✅
- Invalid token → Unauthorized ✅
- `verifyToken` resolver exercised ✅

---

## Medium Priority Issues

### M1 — Branch coverage pre-existing gap (63.1% vs 65% threshold)
- **Source:** `talent-registry.routes.ts` (28.1%), `developer-program.routes.ts` (22.2%), `clara-core/resolvers.ts` (13.0%)
- **Added by:** Prompts 06-09 (not these PRs)
- **Impact:** Jest exits non-zero on coverage run — CI would catch this
- **Fix:** Add branch tests for talent registry routes (happy path already tested; missing: not-found 404, validation errors, authorization checks) and Clara Core GraphQL resolvers

---

## Low Priority Issues

### L1 — Token length side-channel in `verifyClaraServiceToken`
- **File:** `packages/marketplace-sdk/src/auth.ts`
- **Problem:** `if (expected.length !== provided.length) return false` reveals token length via early return timing difference
- **Risk:** Low — this is server-to-server auth, not exposed to end users. Token length is not secret.
- **Fix:** Pad both buffers to equal length before `timingSafeEqual` if strict hardening is desired. Not blocking.

### L2 — Developer portal has no unit tests
- **File:** `packages/developer-portal/`
- **Problem:** No test suite in the Next.js frontend package
- **Risk:** Low for v1 — UI behavior is verified manually; all API integration is tested via backend tests
- **Fix:** Add component tests (React Testing Library) for `TalentRow`, `Sidebar`, `useAuth` in a future pass

---

## Positive Findings

- **All prior HIGH/MEDIUM issues from 20260414-review-prompts-02-03.md are closed.** The B+ review from this morning has been fully remediated.
- **`request-tier.ts` now has 100% branch coverage.** All three auth paths — plaintext key, hashed key, Clerk fallback — are exercised with real function calls. This is exactly the standard required.
- **`verifyClaraServiceToken` is correctly implemented.** Constant-time comparison, fail-fast on missing env var, applied on every subgraph request. Security model matches the architecture doc.
- **Subscriber identity never leaks to talent code.** `parseClaraContext` extracts only session metadata. userId, email, subscription data are not forwarded. Clara mediates the boundary cleanly.
- **Developer portal correctly uses API key auth** — no Clerk SDK dependency in the portal package. Redirects to main app sign-in for authentication. Clean separation.
- **`jest.setup.js` pattern is correct.** Setting `CLARA_VOICE_URL` in test setup allows `resolveModel()` to work without triggering the empty-backend guard while still testing the guard explicitly by mutating `MODELS` in the test.
- **Static schema files for Apollo Router** — pragmatic solution to rover's introspection limitation with auth-gated subgraphs. The correct file-based compose approach.

---

## Corrective Action Required

One unresolved pre-existing issue requires follow-up before `develop` can merge to `main`:

1. **Branch coverage for talent registry + Clara Core resolvers** — `src/features/talent-registry/` and `src/graphql/clara-core/` files need branch test coverage to bring global branch coverage above 65%

See corrective prompt: `prompts/2026/April/14/1-not-started/14-fix-talent-registry-branch-coverage.md`

---

*Review generated by Carruthers — Clara Code Tech Lead*
